# Kaggriculture — development / test dependencies
#
# Includes the runtime requirements and adds the tooling used by the repo:
#   - pytest  (run the test suite: `pytest`)
#   - mypy    (strict type-checking: `mypy`, configured in pyproject.toml)

-r requirements.txt

pytest==9.1.1
mypy==2.3.0
